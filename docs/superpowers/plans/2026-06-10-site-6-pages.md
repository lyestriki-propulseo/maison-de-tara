# Maison de Tara — Site 6 pages : Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer le one-page actuel en site statique 6 pages (Accueil, Atelier, Boutique, Calendrier, Histoire, Contact) aux couleurs/polices de la charte, via partials mutualisés, un configurateur couleur, et des formulaires branchés sur Supabase.

**Architecture:** HTML statique assemblé par un petit script Node (`build.mjs`) qui injecte des partials (`_partials/`) dans des pages sources (`_src/`) et génère les `*.html` servis. Migration couleur/police en un jeu de tokens unique (`tokens.css`). Pages dynamiques (calendrier, lab) en scroll natif (sans Locomotive). Formulaires → Supabase (insert-only, RLS).

**Tech Stack:** HTML/CSS/Vanilla JS, Node ≥18 (ESM, zéro dépendance pour le build), Locomotive Scroll + jQuery + Swiper (hérités Wandau, conservés sur les pages éditoriales), `@supabase/supabase-js` (CDN ESM), Playwright (vérif visuelle, dispo dans l'environnement).

**Spec de référence:** [docs/superpowers/specs/2026-06-10-maison-de-tara-site-6-pages-design.md](../specs/2026-06-10-maison-de-tara-site-6-pages-design.md)

**Répertoire de travail:** `wandau-mdt/` (branche `feat/site-6-pages`).

---

## Structure des fichiers

**Créés :**
- `wandau-mdt/build.mjs` — assembleur d'includes.
- `wandau-mdt/_partials/head.html` — `<head>` mutualisé (tokens, polices charte, marqueurs titre/description par page).
- `wandau-mdt/_partials/header.html` — skip-link + sprite SVG + topbar + header + drawer.
- `wandau-mdt/_partials/footer.html` — footer + coordonnées.
- `wandau-mdt/_partials/scripts.html` — libs complètes (jQuery, Locomotive, Swiper, scripts.js) + drawer (pages éditoriales).
- `wandau-mdt/_partials/scripts-min.html` — drawer seul (pages calendrier/lab, sans preloader/Locomotive).
- `wandau-mdt/_src/{index,atelier,boutique,calendrier,histoire,contact}.html` — sources avec marqueurs d'include.
- `wandau-mdt/css/tokens.css` — source unique couleurs/polices charte.
- `wandau-mdt/js/calendar.js` — rendu calendrier mensuel.
- `wandau-mdt/js/supabase-config.js` — URL projet + anon key.
- `wandau-mdt/js/forms-supabase.js` — soumission formulaires → Supabase.
- `wandau-mdt/data/events.json` — événements du calendrier.
- `wandau-mdt/lab-couleurs.html` + `wandau-mdt/js/lab.js` — configurateur couleur.
- `wandau-mdt/404.html` — page d'erreur.
- `wandau-mdt/tools/visual-check.mjs` — captures multi-viewport (vendorisé).

**Modifiés :**
- `wandau-mdt/css/mdt-override.css` — migration vers tokens charte.
- `wandau-mdt/js/scripts.js` — séparation preloader/Locomotive vs drawer (chargement conditionnel).
- Les 5 pages Wandau brutes (`contact, collections, collection-detail, about, visit, exhibitions`) → réécrites dans `_src/` sur le squelette canonique.

**Convention :** on **n'édite jamais** les `*.html` à la racine de `wandau-mdt/` (générés). On édite `_src/` et `_partials/`, puis `node build.mjs`.

---

## PHASE 0a — Scaffold & extraction (build identique)

But : poser le moteur d'include et extraire la home actuelle en partials **sans rien changer au rendu**.

### Task 0a.1 : Créer l'assembleur build.mjs

**Files:**
- Create: `wandau-mdt/build.mjs`

- [ ] **Step 1 : Écrire build.mjs**

```js
// Assembleur d'includes statique — zéro dépendance.
// Lit _src/*.html, remplace <!-- include: name --> par _partials/name.html,
// écrit les *.html à la racine wandau-mdt/. Ne relit JAMAIS la racine (idempotent).
import { readFileSync, writeFileSync, readdirSync, mkdirSync, existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = dirname(fileURLToPath(import.meta.url));
const SRC = join(ROOT, '_src');
const PARTIALS = join(ROOT, '_partials');
const INCLUDE_RE = /<!--\s*include:\s*([\w-]+)\s*-->/g;

function renderPartial(name, seen) {
  if (seen.has(name)) throw new Error(`Include cyclique détecté: ${name}`);
  const next = new Set(seen).add(name);
  const file = join(PARTIALS, `${name}.html`);
  if (!existsSync(file)) throw new Error(`Partial introuvable: _partials/${name}.html`);
  return readFileSync(file, 'utf8').replace(INCLUDE_RE, (_, n) => renderPartial(n, next));
}

function build() {
  if (!existsSync(SRC)) throw new Error('Dossier _src/ manquant');
  const pages = readdirSync(SRC).filter((f) => f.endsWith('.html'));
  for (const page of pages) {
    const src = readFileSync(join(SRC, page), 'utf8');
    const out = src.replace(INCLUDE_RE, (_, name) => renderPartial(name, new Set()));
    if (INCLUDE_RE.test(out)) throw new Error(`Marqueur include résiduel dans ${page}`);
    writeFileSync(join(ROOT, page), out);
    console.log(`✓ ${page}`);
  }
  console.log(`${pages.length} page(s) assemblée(s).`);
}
build();
```

- [ ] **Step 2 : Créer les dossiers**

Run: `node -e "const fs=require('fs');['_src','_partials'].forEach(d=>fs.mkdirSync('wandau-mdt/'+d,{recursive:true}))"`
Expected: dossiers `wandau-mdt/_src/` et `wandau-mdt/_partials/` créés.

### Task 0a.2 : Extraire la home en partials (verbatim)

**Files:**
- Create: `wandau-mdt/_partials/head.html` (extrait de `index.html:3-34`, sans les balises `<head>`/`</head>`)
- Create: `wandau-mdt/_partials/header.html` (extrait de `index.html:54-177` — skip-link + sprite + topbar + header + drawer)
- Create: `wandau-mdt/_partials/scripts.html` (extrait de `index.html:681-731` — `<!-- JS FILES -->` jusqu'à la fin du script drawer inclus)
- Create: `wandau-mdt/_src/index.html`

- [ ] **Step 1 : Copier head.html** — copier le contenu **exact** de `index.html` lignes 3→34 (l'intérieur de `<head>…</head>`) dans `_partials/head.html`.

- [ ] **Step 2 : Copier header.html** — copier `index.html` lignes 54→177 (du commentaire `HEADER MDT` jusqu'à la fermeture `</div>` du drawer, juste avant `<div class="smooth-scroll">`) dans `_partials/header.html`.

- [ ] **Step 3 : Copier scripts.html** — copier `index.html` lignes 681→731 (de `<!-- JS FILES -->` jusqu'au `</script>` du drawer inclus) dans `_partials/scripts.html`.

- [ ] **Step 4 : Construire _src/index.html** — recréer `index.html` en remplaçant les zones extraites par des marqueurs :

```html
<!doctype html>
<html lang="fr">
<head>
<!-- include: head -->
</head>
<body>
<!-- preloader + page-transition : lignes 36-52 de l'index original, copiées telles quelles ici -->
<!-- include: header -->
<div class="smooth-scroll">
  <!-- ... TOUT le contenu original lignes 179-679 (la .section-wrapper) copié tel quel ... -->
</div>
<!-- include: scripts -->
</body>
</html>
```

> Détail : copier verbatim les lignes 36-52 (preloader) et 179-679 (contenu) depuis l'`index.html` actuel.

- [ ] **Step 5 : Build et vérifier l'identité au rendu**

Run:
```bash
cd wandau-mdt && node build.mjs && cd ..
git diff --stat wandau-mdt/index.html
```
Expected: `node build.mjs` affiche `✓ index.html` ; le `git diff` ne montre **aucune** différence significative (au pire des espaces/fin de ligne). Si des lignes de contenu diffèrent → l'extraction a perdu/dupliqué du markup, corriger avant de continuer.

- [ ] **Step 6 : Commit**

```bash
git add wandau-mdt/build.mjs wandau-mdt/_src/index.html wandau-mdt/_partials/
git commit -m "feat(mdt): assembleur build.mjs + extraction home en partials (rendu identique)"
```

### Task 0a.3 : Test d'idempotence du build

**Files:**
- Create: `wandau-mdt/tools/test-build.mjs`

- [ ] **Step 1 : Écrire le test**

```js
// Vérifie : (1) aucun marqueur include résiduel ; (2) build idempotent (2 builds == même sortie).
import { execSync } from 'node:child_process';
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';
const ROOT = new URL('..', import.meta.url).pathname.replace(/^\/(\w:)/, '$1');
execSync('node build.mjs', { cwd: ROOT });
const snap1 = readdirSync(join(ROOT, '_src')).map((p) => readFileSync(join(ROOT, p), 'utf8'));
execSync('node build.mjs', { cwd: ROOT });
const snap2 = readdirSync(join(ROOT, '_src')).map((p) => readFileSync(join(ROOT, p), 'utf8'));
const leftover = snap2.some((h) => /<!--\s*include:/.test(h));
const stable = JSON.stringify(snap1) === JSON.stringify(snap2);
if (leftover) throw new Error('FAIL: marqueur include résiduel');
if (!stable) throw new Error('FAIL: build non idempotent');
console.log('PASS: build idempotent, aucun marqueur résiduel');
```

- [ ] **Step 2 : Lancer le test**

Run: `node wandau-mdt/tools/test-build.mjs`
Expected: `PASS: build idempotent, aucun marqueur résiduel`

- [ ] **Step 3 : Commit**

```bash
git add wandau-mdt/tools/test-build.mjs
git commit -m "test(mdt): idempotence du build d'includes"
```

---

## PHASE 0b — Squelette canonique, tokens charte & migration

But : un seul gabarit de page (header hors `.smooth-scroll`), tokens charte appliqués, polices swappées, 5 pages Wandau réécrites dans `_src/`, chargement JS conditionnel.

### Task 0b.1 : tokens.css (couleurs + polices charte)

**Files:**
- Create: `wandau-mdt/css/tokens.css`
- Modify: `wandau-mdt/_partials/head.html`

- [ ] **Step 1 : Écrire tokens.css**

```css
/* tokens.css — source unique de la charte Maison de Tara. */
:root {
  /* Couleurs charte */
  --mdt-terracotta: #79301E;   /* accent fort */
  --mdt-rose:       #BA7770;   /* accent doux */
  --mdt-olive:      #8A976C;   /* vert de marque (clair) */
  --mdt-olive-deep: #5A6347;   /* vert foncé pour fonds/texte sombres (dérivé) */
  --mdt-beige:      #F0ECDB;   /* fond / base */
  --mdt-ink:        #2C2419;   /* texte courant */
  /* Polices charte */
  --mdt-font-display: "Playfair Display", Georgia, serif;
  --mdt-font-script:  "La Belle Aurore", cursive;
  --mdt-font-body:    "Inter", system-ui, sans-serif; /* provisoire, finalisé via le lab */
}
```

- [ ] **Step 2 : Brancher tokens.css + polices charte dans head.html** — dans `_partials/head.html` : (a) remplacer le `<link>` Google Fonts Cormorant/Inter/Caveat par Playfair Display + La Belle Aurore (+ Inter conservé en body provisoire) ; (b) ajouter `<link rel="stylesheet" href="css/tokens.css">` **avant** `mdt-override.css`.

```html
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400&family=La+Belle+Aurore&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="css/tokens.css">
```

- [ ] **Step 3 : Build + vérifier le chargement des polices**

Run: `cd wandau-mdt && node build.mjs && cd ..`
Then: servir + ouvrir l'accueil (cf. Task de vérif visuelle 0b.5) et confirmer en DevTools que Playfair Display est chargée (onglet Network → fonts).

### Task 0b.2 : Migration des tokens existants (pas de doublon)

**Files:**
- Modify: `wandau-mdt/css/mdt-override.css`

- [ ] **Step 1 : Auditer tous les usages de l'ancienne palette**

Run:
```bash
cd "c:/Users/cleme/Desktop/Propulseo/Projet/maison-de-tara"
grep -rnoE "--mdt-(sable|sable-warm|sable-deep|terracotta|sauge|sauge-light|terra-dark|noir|noir-soft|laiton)[a-z-]*" wandau-mdt/css wandau-mdt/_src wandau-mdt/_partials | sort | uniq -c
```
Expected: la liste exhaustive des variables héritées et de leurs occurrences.

- [ ] **Step 2 : Remapper le `:root` de mdt-override.css vers les tokens charte (alias)** — dans le `:root` de `mdt-override.css`, remplacer les **valeurs** par des références charte. Règle :
  - `--mdt-sable: var(--mdt-beige);`
  - `--mdt-terracotta: var(--mdt-terracotta-charte);` → renommer plutôt : poser directement `--mdt-terracotta: #79301E` dans tokens.css et **supprimer** la redéfinition locale.
  - `--mdt-sauge` : ⚠️ **ne pas remapper naïvement vers olive clair**. Là où `--mdt-sauge` servait de **vert sombre** (texte sur fond clair, fond de footer), remapper vers `--mdt-olive-deep` ou `--mdt-ink` ; là où il servait d'aplat de marque, vers `--mdt-olive`. Trancher **par occurrence** d'après le grep Step 1.

- [ ] **Step 3 : Vérifier qu'aucun hex Wandau/ancien ne subsiste**

Run:
```bash
grep -rniE "#(4A5D2E|C75B3C|F4EDE0|7A8043)" wandau-mdt/css wandau-mdt/_src wandau-mdt/_partials || echo "OK: aucune ancienne couleur en dur"
```
Expected: `OK: aucune ancienne couleur en dur` (ou seulement dans des commentaires explicites).

- [ ] **Step 4 : Commit**

```bash
git add wandau-mdt/css/tokens.css wandau-mdt/css/mdt-override.css wandau-mdt/_partials/head.html
git commit -m "feat(mdt): tokens charte (couleurs+polices) + migration sans doublon"
```

### Task 0b.3 : Séparer drawer / Locomotive (chargement conditionnel)

**Files:**
- Modify: `wandau-mdt/js/scripts.js`
- Create: `wandau-mdt/_partials/scripts-min.html`

- [ ] **Step 1 : Garde Locomotive dans scripts.js** — au début de l'IIFE de `scripts.js`, encadrer l'init Locomotive + le preloader d'une garde :

```js
var smoothEl = document.querySelector('.smooth-scroll');
if (!smoothEl) { /* page sans smooth-scroll : ne pas init Locomotive ni le preloader bloquant */ }
```
Concrètement : n'exécuter `new LocomotiveScroll({...})` et la logique preloader que si `smoothEl` existe ; sinon retirer la classe preloader / `body.overflow` immédiatement.

- [ ] **Step 2 : Créer scripts-min.html** — drawer seul (le script inline vanilla des lignes 692-730 de l'index original), **sans** jQuery/Locomotive/Swiper/scripts.js :

```html
<!-- JS minimal : drawer uniquement (pages calendrier / lab) -->
<script>
(function () {
  var drawer = document.getElementById('drawer');
  var burger = document.querySelector('.burger');
  if (!drawer || !burger) return;
  var sel = 'a[href], button:not([disabled]), input, [tabindex]:not([tabindex="-1"])';
  var prev = null;
  function open(){ prev=document.activeElement; drawer.hidden=false; void drawer.offsetWidth; drawer.classList.add('is-open'); document.body.classList.add('is-drawer-open'); burger.setAttribute('aria-expanded','true'); var f=drawer.querySelector(sel); if(f)f.focus(); }
  function close(){ drawer.classList.remove('is-open'); document.body.classList.remove('is-drawer-open'); burger.setAttribute('aria-expanded','false'); setTimeout(function(){ drawer.hidden=true; if(prev&&prev.focus)prev.focus(); },300); }
  burger.addEventListener('click', open);
  drawer.querySelectorAll('[data-drawer-close]').forEach(function(el){ el.addEventListener('click', close); });
  document.addEventListener('keydown', function(e){ if(e.key==='Escape'&&drawer.classList.contains('is-open'))close(); });
  window.addEventListener('resize', function(){ if(window.innerWidth>900&&drawer.classList.contains('is-open'))close(); });
})();
</script>
```

- [ ] **Step 3 : Mettre à jour _partials/scripts.html** — y conserver les libs complètes + `scripts.js` + le même bloc drawer (pages éditoriales). Le drawer doit donc vivre dans **les deux** partials (dupliqué volontairement, ~15 lignes) OU être extrait en `js/drawer.js` chargé par les deux. **Choix : extraire `js/drawer.js`** et l'inclure via `<script src="js/drawer.js"></script>` dans `scripts.html` ET `scripts-min.html` (DRY).

- [ ] **Step 4 : Build + vérifier la home (Locomotive OK) et une page min plus tard.**

Run: `cd wandau-mdt && node build.mjs && cd ..`
Expected: build OK ; la home garde son smooth-scroll, aucune régression.

- [ ] **Step 5 : Commit**

```bash
git add wandau-mdt/js/scripts.js wandau-mdt/js/drawer.js wandau-mdt/_partials/scripts.html wandau-mdt/_partials/scripts-min.html
git commit -m "refactor(mdt): drawer extrait (DRY) + Locomotive/preloader conditionnels"
```

### Task 0b.4 : Nav multi-pages + footer partial

**Files:**
- Modify: `wandau-mdt/_partials/header.html`
- Create: `wandau-mdt/_partials/footer.html`

- [ ] **Step 1 : Transformer les ancres en liens de pages** — dans `header.html`, remplacer les `href="#atelier"` etc. par les vraies pages :

```html
<a href="index.html">Accueil</a>
<a href="atelier.html">L'Atelier</a>
<a href="boutique.html">La Boutique</a>
<a href="calendrier.html">Le Calendrier</a>
<a href="histoire.html">Notre Histoire</a>
<a href="contact.html">Contact</a>
```
(idem dans le drawer mobile). Retirer le badge « Bientôt » de Boutique. Mettre à jour la cible du CTA « Réserver » → `atelier.html#reserver`.

- [ ] **Step 2 : Extraire le footer** — déplacer le footer actuel de la home (repérer la section `<footer>`/`.footer` dans le contenu) dans `_partials/footer.html`, avec les **vraies coordonnées** : `contact@maisondetara.com`, `+33 6 50 53 51 49`, 1 Rue Gabriel Péri 92250 La Garenne-Colombes, Insta `@maison_de_tara`. Remplacer son appel par `<!-- include: footer -->` dans `_src/index.html`.

- [ ] **Step 3 : Corriger les coordonnées de la topbar** — dans `header.html`, remplacer `01 47 XX XX XX` → `+33 6 50 53 51 49` et `hello@maisondetara.com` → `contact@maisondetara.com`.

- [ ] **Step 4 : Build + commit**

Run: `cd wandau-mdt && node build.mjs && cd ..`
```bash
git add wandau-mdt/_partials/header.html wandau-mdt/_partials/footer.html wandau-mdt/_src/index.html
git commit -m "feat(mdt): nav 6 pages + footer mutualisé + coordonnées réelles"
```

### Task 0b.5 : Vendoriser l'outil de vérif visuelle

**Files:**
- Create: `wandau-mdt/tools/visual-check.mjs`

- [ ] **Step 1 : Copier visual-check.mjs** depuis `Interne Propulseo/Pastel Auto-Fix/visual-check.mjs` vers `wandau-mdt/tools/visual-check.mjs`, en adaptant l'URL de base au serveur statique local (cf. le serveur PowerShell du repo). Si Playwright n'est pas installé localement : `npm i -D playwright && npx playwright install chromium` à la racine, OU utiliser le Playwright MCP de l'environnement.

- [ ] **Step 2 : Capturer la home (référence avant/après)**

Run: démarrer le serveur statique du repo, puis `node wandau-mdt/tools/visual-check.mjs index.html`
Expected: captures mobile/tablette/desktop de l'accueil générées (référence de non-régression).

- [ ] **Step 3 : Commit**

```bash
git add wandau-mdt/tools/visual-check.mjs
git commit -m "chore(mdt): vendorise visual-check (captures multi-viewport)"
```

### Task 0b.6 : Réécrire les 5 pages Wandau sur le squelette canonique

**Files:**
- Create: `wandau-mdt/_src/{atelier,boutique,calendrier,histoire,contact}.html`
- Référence: `wandau-mdt/{visit,collections,exhibitions,about,contact}.html`

Pour **chaque** page (répéter les steps) — on bâtit une coquille canonique identique à la home : `head` inclus, header **hors** `.smooth-scroll`, contenu dans `.smooth-scroll > .section-wrapper[data-scroll-section]`, scripts inclus.

- [ ] **Step 1 (atelier) : créer `_src/atelier.html`** sur ce gabarit :

```html
<!doctype html>
<html lang="fr">
<head>
<!-- include: head -->
<title>L'Atelier · Maison de Tara</title>
</head>
<body>
<!-- include: header -->
<div class="smooth-scroll">
  <div class="section-wrapper" data-scroll-section>
    <!-- contenu Atelier (rempli en Phase 5) -->
  </div>
</div>
<!-- include: scripts -->
</body>
</html>
```

- [ ] **Step 2 : idem pour boutique, histoire, contact** (même gabarit, `<title>` adapté). **Pour `calendrier`** : utiliser `<!-- include: scripts-min -->` au lieu de `scripts`, et **ne pas** envelopper le contenu dans `.smooth-scroll` (scroll natif, cf. Phase 6).

- [ ] **Step 3 : Build — 6 pages générées**

Run: `cd wandau-mdt && node build.mjs && cd ..`
Expected: `✓ index.html … ✓ contact.html` (6 pages), aucune page blanche.

- [ ] **Step 4 : Vérifier l'absence de hex Wandau résiduel sur les 6 pages**

Run: `grep -rniE "#(4A5D2E|C75B3C|F4EDE0)" wandau-mdt/*.html || echo "OK"`
Expected: `OK`.

- [ ] **Step 5 : Commit**

```bash
git add wandau-mdt/_src/
git commit -m "feat(mdt): 6 coquilles de pages sur squelette canonique"
```

---

## PHASE 1 — Lab configurateur couleur

But : une page autonome où Tara choisit, par zone, parmi 4 options, avec rendu live ; export des tokens retenus.

### Task 1.1 : Page lab + mécanique de configuration

**Files:**
- Create: `wandau-mdt/lab-couleurs.html`
- Create: `wandau-mdt/js/lab.js`

- [ ] **Step 1 : Écrire lab-couleurs.html** (autonome, sans scripts.js/Locomotive/preloader) avec un échantillon de rendu réel (hero + section + boutons + titres) et les panneaux de zones :

```html
<!doctype html>
<html lang="fr">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>Lab couleurs · Maison de Tara</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600&family=La+Belle+Aurore&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
<link rel="stylesheet" href="css/tokens.css">
<style>
  /* mise en page du lab : colonne contrôles + preview ; la preview lit des --lab-* */
  .lab{display:grid;grid-template-columns:320px 1fr;gap:24px}
  .preview{--lab-bg:var(--mdt-olive);--lab-header:var(--mdt-beige);--lab-cta:var(--mdt-terracotta);--lab-title:var(--mdt-terracotta);--lab-body:var(--mdt-font-body);--lab-pattern-opacity:0.18}
  .preview .hero{position:relative;overflow:hidden;background:var(--lab-bg);font-family:var(--lab-body)}
  .preview .hero::before{content:"";position:absolute;inset:0;background:url(../assets-premium-lp/wallpaper-tara-green.png) center/720px repeat;opacity:var(--lab-pattern-opacity);mix-blend-mode:screen;pointer-events:none}
  .preview .hero h1{color:var(--lab-title);font-family:var(--mdt-font-display)}
  .preview .cta{background:var(--lab-cta)}
  .swatch{cursor:pointer;border:2px solid transparent}
  .swatch[aria-pressed="true"]{border-color:var(--mdt-ink)}
</style>
</head>
<body>
<div class="lab">
  <aside class="controls"><!-- zones injectées par lab.js --></aside>
  <main class="preview" id="preview">
    <section class="hero"><h1>Bienvenue à la Maison de Tara</h1><a class="cta">Réserver un moment</a></section>
    <section class="sample"><h2>À découvrir à la maison</h2><p>Texte d'exemple…</p></section>
  </main>
</div>
<aside class="export" id="export"><h3>Palette retenue</h3><pre id="export-out"></pre></aside>
<script src="js/lab.js"></script>
</body>
</html>
```

- [ ] **Step 2 : Écrire js/lab.js** — définit les zones, génère les boutons, applique les `--lab-*` au clic, met à jour l'export :

```js
// Configurateur : chaque zone applique une CSS custom property sur #preview.
const ZONES = [
  { key: 'bg',    label: 'Fond vert',  prop: '--lab-bg',
    options: ['#8A976C', '#9AA67E', '#A7B190', '#7C8A5E'] },
  { key: 'header',label: 'Header',     prop: '--lab-header',
    options: ['#F0ECDB', '#E7E0C8', '#8A976C', '#FBF9F1'] },
  { key: 'cta',   label: 'Boutons',    prop: '--lab-cta',
    options: ['#79301E', '#BA7770', '#8A976C', 'transparent'] },
  { key: 'title', label: 'Titres',     prop: '--lab-title',
    options: ['#79301E', '#5A6347', '#2C2419', '#BA7770'] },
  { key: 'body',  label: 'Police corps',prop: '--mdt-font-body',
    options: ['Inter, sans-serif', 'Mulish, sans-serif', 'Karla, sans-serif', 'Nunito Sans, sans-serif'] },
  { key: 'pattern', label: 'Pattern floral (opacité)', prop: '--lab-pattern-opacity', // zone secondaire (§4)
    options: ['0.12', '0.18', '0.24', '0.30'] },
];
const preview = document.getElementById('preview');
const out = document.getElementById('export-out');
const chosen = {};
const controls = document.querySelector('.controls');

for (const z of ZONES) {
  const block = document.createElement('div');
  block.innerHTML = `<h4>${z.label}</h4>`;
  z.options.forEach((val, i) => {
    const b = document.createElement('button');
    b.className = 'swatch';
    b.style.background = val.startsWith('#') ? val : '#ddd';
    b.textContent = val.startsWith('#') ? '' : val.split(',')[0];
    b.setAttribute('aria-pressed', i === 0 ? 'true' : 'false');
    b.addEventListener('click', () => {
      preview.style.setProperty(z.prop, val);
      chosen[z.key] = val;
      block.querySelectorAll('.swatch').forEach((s) => s.setAttribute('aria-pressed', 'false'));
      b.setAttribute('aria-pressed', 'true');
      render();
    });
    block.appendChild(b);
  });
  controls.appendChild(block);
  chosen[z.key] = z.options[0];
}
function render() {
  out.textContent = ZONES.map((z) => `${z.prop}: ${chosen[z.key]};`).join('\n');
}
render();
```

- [ ] **Step 3 : Vérifier le live preview**

Run: servir le repo, ouvrir `wandau-mdt/lab-couleurs.html`, cliquer chaque zone.
Expected: la preview change en live ; l'encart « Palette retenue » liste les valeurs choisies.

- [ ] **Step 4 : Capture pour Tara + commit**

Run: `node wandau-mdt/tools/visual-check.mjs lab-couleurs.html`
```bash
git add wandau-mdt/lab-couleurs.html wandau-mdt/js/lab.js
git commit -m "feat(mdt): lab configurateur couleur (4 choix/zone, live preview)"
```

> 🚦 **Checkpoint Tara** : présenter le lab → Tara verrouille la palette. Reporter les valeurs retenues dans `css/tokens.css` (Task 1.2).

### Task 1.2 : Verrouiller la palette retenue

- [ ] **Step 1 :** Mettre à jour `css/tokens.css` avec les valeurs choisies par Tara (et `--mdt-font-body` final).
- [ ] **Step 2 :** `cd wandau-mdt && node build.mjs && cd ..`, re-capturer la home.
- [ ] **Step 3 : Commit** `git commit -am "feat(mdt): palette validée par Tara verrouillée dans tokens"`.

---

## PHASE 2 — Accueil (repaint + textes)

But : appliquer la palette + les textes Pastel sur la home.

### Task 2.1 : Textes de l'accueil

**Files:**
- Modify: `wandau-mdt/_src/index.html`

- [ ] **Step 1 : Remplacer les textes** d'après les retours Pastel (source : `annotations-tara-922080.json`), tous en **« je »** :
  - H1 (#18) → `Bienvenue à la Maison de Tara`
  - Sous-titre hero (#19), tagline (#21) « peinture sur céramique – salon de thé – boutique déco maison »
  - Titres sections : #24 « À découvrir à la maison », #25 « L'envie de créer », #29 « Une sélection pour la maison », #31 « Pour accompagner le moment »
  - Paragraphes #26 (atelier), #30 (boutique), #33 (café) — copier le texte exact des retours.
  - Newsletter (#49).
- [ ] **Step 2 : Teaser boutique #50** — insérer un encart « Passer à la maison – découvrir la boutique déco » (texte fourni dans #50) entre l'univers atelier et la zone rendez-vous, menant à `boutique.html`.
- [ ] **Step 3 : Ajustements directs** — #8 (logo plus grand à l'arrivée puis réduit au scroll, via classe), #22 (recentrer le SVG), #23 (logo), #3 (retirer bouton redondant header), #16 (box réservation → bouton flottant vers `atelier.html#reserver`).
- [ ] **Step 4 : Build + vérif visuelle**

Run: `cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs index.html`
Expected: accueil aux couleurs charte + textes Tara, responsive OK (mobile/tablette/desktop), rien de cassé.

- [ ] **Step 5 : Commit** `git commit -am "feat(mdt): accueil — textes Tara (je) + palette + teaser boutique #50"`.

> 🚦 **Checkpoint Tara** sur l'accueil.

---

## PHASE 3 — Contact

But : page Contact complète (texte « je », accès, horaires d'ouverture, FAQ, carte corrigée, formulaire → Supabase).

### Task 3.1 : Structure & contenu Contact (FR, « je »)

**Files:**
- Modify: `wandau-mdt/_src/contact.html`
- Référence: `wandau-mdt/contact.html` (markup form + boxes + iframe)

- [ ] **Step 1 : Reprendre les blocs** du `contact.html` Wandau (formulaire `#contactForm`, boxes infos, iframe) dans `_src/contact.html`, **traduits en FR**.
- [ ] **Step 2 : Bloc « Venir à la maison »** (doc Contact) : Train (gare La Garenne-Colombes / Paris Saint-Lazare ligne L), Tram T2 Les Fauvelles, Bus 163/164/178/278, Voiture + parking.
- [ ] **Step 3 : Horaires d'ouverture de la maison** (par jour, doc Contact) : Lun fermé · Mar 12h-18h · Mer 9h-18h · Jeu 9h-21h · Ven 9h-21h · Sam 9h-21h · Dim 9h-15h.
- [ ] **Step 4 : Corriger l'iframe Google Maps** → adresse réelle `1 Rue Gabriel Péri, 92250 La Garenne-Colombes` (remplacer l'URL pointant sur Kiev).
- [ ] **Step 5 : FAQ** (doc Contact, en « je ») avec le cross-link « comment savoir ce qui se passe » → `calendrier.html` + newsletter (ferme #48).
- [ ] **Step 6 : Build + vérif visuelle + commit.**

```bash
cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs contact.html
git commit -am "feat(mdt): page Contact (FR, je) — accès, horaires, FAQ, carte corrigée"
```

### Task 3.2 : Backend Supabase + branchement du formulaire

**Files:**
- Create: `wandau-mdt/js/supabase-config.js`, `wandau-mdt/js/forms-supabase.js`
- Modify: `wandau-mdt/_src/contact.html`

- [ ] **Step 1 : Créer la table Supabase** — table `submissions` (`id uuid default gen_random_uuid()`, `type text`, `payload jsonb`, `created_at timestamptz default now()`), **RLS activée**, **policy `insert` only** pour le rôle `anon`, **aucune** policy `select`. (Via MCP Supabase `apply_migration` ou le dashboard.)

- [ ] **Step 2 : Config client**

```js
// js/supabase-config.js — valeurs PUBLIQUES (anon). RLS insert-only obligatoire côté DB.
export const SUPABASE_URL = 'https://<PROJET>.supabase.co';
export const SUPABASE_ANON_KEY = '<ANON_KEY>';
```

- [ ] **Step 3 : Module de soumission**

```js
// js/forms-supabase.js
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import { SUPABASE_URL, SUPABASE_ANON_KEY } from './supabase-config.js';
const sb = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

export function bindForm(formEl, type) {
  formEl.addEventListener('submit', async (e) => {
    e.preventDefault();
    const payload = Object.fromEntries(new FormData(formEl).entries());
    const btn = formEl.querySelector('[type="submit"]');
    if (btn) btn.disabled = true;
    const { error } = await sb.from('submissions').insert({ type, payload });
    const msg = formEl.querySelector('.form-msg') || formEl;
    msg.textContent = error ? "Une erreur est survenue, réessayez." : "Merci, votre message est bien envoyé.";
    if (!error) formEl.reset();
    if (btn) btn.disabled = false;
  });
}
```

- [ ] **Step 4 : Brancher sur la page Contact** — dans `_src/contact.html`, ajouter en bas :

```html
<script type="module">
  import { bindForm } from './js/forms-supabase.js';
  bindForm(document.getElementById('contactForm'), 'contact');
</script>
```
Et ajouter un `<p class="form-msg" role="status"></p>` dans le formulaire.

- [ ] **Step 5 : Test manuel** — servir, soumettre le formulaire, vérifier l'insertion (MCP Supabase `execute_sql: select count(*) from submissions`) et qu'aucune lecture anon n'est possible.

Expected: 1 ligne insérée ; `select` en anon refusé (RLS).

- [ ] **Step 6 : Commit** (⚠️ l'anon key est publique mais vérifier qu'aucune `service_role` n'est committée)

```bash
git add wandau-mdt/js/supabase-config.js wandau-mdt/js/forms-supabase.js wandau-mdt/_src/contact.html
git commit -m "feat(mdt): formulaire Contact → Supabase (insert-only, RLS)"
```

> 🚦 **Checkpoint Tara** sur Contact.

---

## PHASE 4 — Boutique

But : page Boutique (récit « je » + grille déco), couleurs charte.

### Task 4.1 : Page Boutique

**Files:**
- Modify: `wandau-mdt/_src/boutique.html`
- Référence: `wandau-mdt/collections.html` (grille 3 col), `wandau-mdt/collection-detail.html` (fiche)

- [ ] **Step 1 :** Reprendre la grille 3 colonnes (`.collection-box`) de `collections.html` dans `_src/boutique.html`, **traduite FR**, adaptée déco/textiles (sans prix/panier).
- [ ] **Step 2 :** Poser le **texte récit Boutique** (doc `page boutique.pptx`, déjà en « je ») en intro de page. Préparer **une version resserrée à côté** (commentaire HTML ou bloc alterné) pour le choix de Tara.
- [ ] **Step 3 :** (optionnel) fiche détail à partir de `collection-detail.html` si une pièce est mise en avant.
- [ ] **Step 4 : Build + vérif visuelle + commit.**

```bash
cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs boutique.html
git commit -am "feat(mdt): page Boutique (récit je + grille déco)"
```

> 🚦 **Checkpoint Tara** sur Boutique.

---

## PHASE 5 — Atelier

But : page Atelier (4 étapes, réservation → Supabase, privatisation).

### Task 5.1 : Section « comment ça se passe » (4 étapes)

**Files:**
- Modify: `wandau-mdt/_src/atelier.html`
- Référence: `premium-section-lab.html` `#process` (variante A, déjà 4 étapes)

- [ ] **Step 1 :** Reprendre le bloc `#process` (variante A « 4 étapes claires ») de `premium-section-lab.html` (racine) dans `_src/atelier.html` — **réécrire les chemins d'assets** (racine → `wandau-mdt/`).
- [ ] **Step 2 :** Remplacer le texte des 4 étapes par (retours #38-44, en « je ») : **1. Réserver votre session** · **2. Peindre à votre rythme** · **3. Me confier votre pièce** · **4. Revenir la retrouver** (textes exacts des retours/doc).
- [ ] **Step 3 :** Reprendre l'**accordéon FAQ** + la **liste à icônes horaires d'ateliers** depuis `wandau-mdt/visit.html`. Créneaux ateliers (doc) : Mar 13h/16h · Mer 10h/13h/16h · Jeu & Ven & Sam 10h/13h/16h/19h · Dim 10h/13h.
- [ ] **Step 4 : Build + vérif visuelle + commit.**

```bash
cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs atelier.html
git commit -am "feat(mdt): Atelier — 4 étapes (#process) + FAQ + créneaux"
```

### Task 5.2 : Formulaire de réservation → Supabase

**Files:**
- Modify: `wandau-mdt/_src/atelier.html`

- [ ] **Step 1 :** Ajouter une section `#reserver` avec un formulaire : **date**, **créneau** (`<select>` alimenté des créneaux ci-dessus), **nb participants** (1-8). Sous le select, note : « **Au-delà de 8, appelez-moi ou passez à la boutique** » (#17). Mentionner l'**acompte 6 €/personne**.
- [ ] **Step 2 :** Brancher via `bindForm(document.getElementById('reservationForm'), 'reservation')` (réutilise `js/forms-supabase.js`).
- [ ] **Step 3 :** Section **privatisation** : sous-titre **#47** « Privatisez la maison pour créer de beaux souvenirs. » ; #45 (réduire la taille des images pour tenir dans la page + photo « other group events »). (#46 anniversaires enfants : **hors scope**, différé.)
- [ ] **Step 4 : Test soumission réservation** (insert Supabase type `reservation`) + vérif visuelle + commit.

```bash
cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs atelier.html
git commit -am "feat(mdt): Atelier — réservation → Supabase + privatisation"
```

> 🚦 **Checkpoint Tara** sur Atelier.

---

## PHASE 6a — Calendrier (rendu)

But : grille mensuelle vanilla qui pose les events d'un fichier JSON sur les bonnes dates (scroll natif).

### Task 6a.1 : Données + module de rendu

**Files:**
- Create: `wandau-mdt/data/events.json`, `wandau-mdt/js/calendar.js`
- Modify: `wandau-mdt/_src/calendrier.html`

- [ ] **Step 1 : events.json (exemple)**

```json
[
  { "date": "2026-10-03", "titre": "Atelier découverte", "type": "atelier", "description": "Session 2h, débutants bienvenus.", "lienReservation": "atelier.html#reserver" },
  { "date": "2026-10-12", "titre": "Brunch & céramique", "type": "evenement", "description": "Brunch + peinture libre." }
]
```

- [ ] **Step 2 : calendar.js — rendu d'un mois**

```js
// Rendu d'une grille mensuelle 7 colonnes + events posés sur les dates.
const MOIS = ['Janvier','Février','Mars','Avril','Mai','Juin','Juillet','Août','Septembre','Octobre','Novembre','Décembre'];
const JOURS = ['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'];

export async function initCalendar(rootId) {
  const root = document.getElementById(rootId);
  const events = await fetch('data/events.json').then((r) => r.json());
  const byDate = {};
  for (const e of events) (byDate[e.date] ||= []).push(e);
  // état : on part du 1er mois ayant un event, sinon un mois fixe (pas de Date.now ici → passé en data si besoin)
  let [year, month] = (events[0]?.date || '2026-10-01').split('-').map(Number);
  month -= 1;

  function render() {
    const first = new Date(year, month, 1);
    const startCol = (first.getDay() + 6) % 7; // lundi = 0
    const days = new Date(year, month + 1, 0).getDate();
    let cells = '';
    for (let i = 0; i < startCol; i++) cells += '<div class="cal-cell cal-empty"></div>';
    for (let d = 1; d <= days; d++) {
      const iso = `${year}-${String(month + 1).padStart(2, '0')}-${String(d).padStart(2, '0')}`;
      const evs = byDate[iso] || [];
      const dot = evs.length ? `<span class="cal-dot" data-date="${iso}">${evs.length}</span>` : '';
      cells += `<button class="cal-cell${evs.length ? ' has-event' : ''}" data-date="${iso}"><span class="cal-num">${d}</span>${dot}</button>`;
    }
    root.innerHTML = `
      <div class="cal-head">
        <button class="cal-prev" aria-label="Mois précédent">‹</button>
        <h2>${MOIS[month]} ${year}</h2>
        <button class="cal-next" aria-label="Mois suivant">›</button>
      </div>
      <div class="cal-grid cal-weekdays">${JOURS.map((j) => `<div>${j}</div>`).join('')}</div>
      <div class="cal-grid cal-days">${cells}</div>
      <div class="cal-detail" id="cal-detail" hidden></div>`;
    root.querySelector('.cal-prev').onclick = () => { month--; if (month < 0) { month = 11; year--; } render(); };
    root.querySelector('.cal-next').onclick = () => { month++; if (month > 11) { month = 0; year++; } render(); };
    root.querySelectorAll('.cal-cell.has-event').forEach((c) => {
      c.onclick = () => showDetail(c.dataset.date, byDate[c.dataset.date]);
    });
  }
  function showDetail(date, evs) {
    const box = root.querySelector('#cal-detail');
    box.hidden = false;
    box.innerHTML = evs.map((e) => `
      <article class="exhibition-box">
        <h3>${e.titre}</h3><p class="cal-date">${date}</p><p>${e.description || ''}</p>
        ${e.lienReservation ? `<a class="btn btn--primary" href="${e.lienReservation}">Réserver</a>` : ''}
      </article>`).join('');
  }
  render();
}
```

- [ ] **Step 3 : Câbler dans _src/calendrier.html** (scroll natif, `scripts-min`)

```html
<section class="cal-section"><div class="mdt-container"><div id="calendar"></div></div></section>
<script type="module">import { initCalendar } from './js/calendar.js'; initCalendar('calendar');</script>
```

- [ ] **Step 4 : Vérifier le rendu d'un mois**

Run: servir, ouvrir `calendrier.html`.
Expected: la grille du mois s'affiche, les events sont sur les bonnes dates (pastille), pas de page blanche (scroll natif, pas de Locomotive).

- [ ] **Step 5 : Commit**

```bash
git add wandau-mdt/data/events.json wandau-mdt/js/calendar.js wandau-mdt/_src/calendrier.html
git commit -m "feat(mdt): calendrier — grille mensuelle + events.json"
```

## PHASE 6b — Calendrier (interactions)

### Task 6b.1 : Navigation & panneau détail

- [ ] **Step 1 :** Vérifier la nav mois ±1 (déjà câblée dans `calendar.js`) et le panneau détail au clic (style `.exhibition-box`).
- [ ] **Step 2 : Styliser** `.cal-grid`, `.cal-cell`, `.has-event`, `.cal-detail` dans `mdt-override.css` aux couleurs charte (olive/terracotta/beige).
- [ ] **Step 3 : Vérif visuelle multi-viewport + commit.**

```bash
cd wandau-mdt && node build.mjs && cd .. && node wandau-mdt/tools/visual-check.mjs calendrier.html
git commit -am "feat(mdt): calendrier — nav mois + panneau détail + style charte"
```

> 🚦 **Checkpoint Tara** sur Calendrier.

---

## PHASE 7 — Histoire (coquille)

But : structure prête (1 des 4 layouts), **non liée en nav / noindex** tant que le texte manque.

### Task 7.1 : Coquille Histoire

**Files:**
- Modify: `wandau-mdt/_src/histoire.html`
- Référence: `about-concept-previews.html` (variantes A-D)

- [ ] **Step 1 :** Choisir une des 4 variantes de `about-concept-previews.html` (proposer à Tara : A Maison de famille / B Manifeste / C Lettre de Tara / D Magazine) et reprendre son markup dans `_src/histoire.html` (chemins d'assets réécrits), aux couleurs charte.
- [ ] **Step 2 :** Texte = placeholder clairement marqué (pas de lorem publiable). Ajouter `<meta name="robots" content="noindex">` et **retirer le lien Histoire de la nav** (header/drawer/footer) tant que le texte de Tara n'est pas livré.
- [ ] **Step 3 : Build + commit.**

```bash
cd wandau-mdt && node build.mjs && cd ..
git commit -am "feat(mdt): coquille Histoire (noindex, hors nav) en attente texte Tara"
```

> ⛔ **Bloquant nommé :** texte page Histoire (Tara).

---

## PHASE 8 — Finitions & mise en prod

But : SEO/meta par page, favicon charte, 404, accessibilité, perf images.

### Task 8.1 : Meta/OG par page

- [ ] **Step 1 :** Dans chaque `_src/*.html`, vérifier `<title>` + ajouter `<meta name="description">` + balises Open Graph spécifiques. (Le `head.html` partagé fournit le commun ; le spécifique va après l'include dans chaque page.)
- [ ] **Step 2 :** Build + commit.

### Task 8.2 : Favicon charte + 404

**Files:**
- Create: `wandau-mdt/404.html`
- Modify: `wandau-mdt/_partials/head.html`

- [ ] **Step 1 :** Générer un favicon depuis l'icône tulipe de la charte (`1- Charte Graphique/Icones/Icone tulipe/...png`) → `wandau-mdt/ico/`. Mettre à jour le `<link rel="shortcut icon">`.
- [ ] **Step 2 :** Créer `_src/404.html` (sur le squelette canonique, message + lien accueil) → build.
- [ ] **Step 3 : Commit.**

### Task 8.3 : Passe accessibilité + perf images

- [ ] **Step 1 :** Vérifier `alt` sur toutes les images, `label`/`aria` sur les champs de formulaire, contrastes (palette charte) — corriger.
- [ ] **Step 2 :** Optimiser/redimensionner les images lourdes héritées (hero, grille boutique) — formats web, dimensions raisonnables.
- [ ] **Step 3 : Vérif visuelle finale des 6 pages + commit.**

```bash
git commit -am "chore(mdt): finitions — meta/OG, favicon tulipe, 404, a11y, perf images"
```

> 🚦 **Checkpoint Tara** final.

---

## Notes d'exécution

- **Ordre des dépendances :** 0a → 0b → 1 (palette) → 2..7 → 8. Les pages (3,4,5,6) sont indépendantes entre elles une fois 0b+1 faits ; elles peuvent être parallélisées si besoin.
- **Toujours** `node build.mjs` après édition de `_src/` ou `_partials/` ; **ne jamais** éditer les `*.html` générés.
- **Secrets :** seule l'**anon key** Supabase est côté client (OK avec RLS insert-only). **Jamais** de `service_role` dans le repo.
- **Type/CSS check :** ce projet est statique (pas de tsc). « Vert » = build OK + `grep` couleurs + captures visual-check sans régression.
